# BUG-02 Evidence

Screenshots documenting the checkout flow used to reproduce the whitespace-only name validation issue.

| File | Description |
|---|---|
| `01-checkout-form-before.png` | Checkout form with whitespace-only values entered into First Name / Last Name before submission. *(Not included in this repo copy — see note below.)* |
| `02-order-confirmed.png` | Order successfully confirmed despite whitespace-only names, proving the validation gap. |

> **Note:** The "before" screenshot referenced in the bug report was provided during testing but wasn't recovered when this repo was assembled. Drop the original file into this folder to complete the evidence set — the bug report already links to it by filename.
